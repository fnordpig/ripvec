{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'set -eo pipefail; root=\"${CLAUDE_PLUGIN_ROOT}\"; case \"$root\" in \"\"|*\"$\"*) root=\"$PWD\" ;; esac; exec bash \"$root/hooks/scripts/check-install.sh\"'"
          }
        ]
      }
    ]
  }
}
