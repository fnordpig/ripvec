[project]
name = "ripvec-benchmarks"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "sentence-transformers>=3.0",
    "torch>=2.1",
    "numpy",
    "einops",
]

[project.scripts]
benchmark-modernbert = "benchmark_modernbert:main"
